"use client";
import { motion } from "framer-motion";
// Drawer/index.tsx
const Drawer = () => {
    return (
      <motion.div className="fixed inset-0 bg-white">
        Test
      </motion.div>
    );
  };